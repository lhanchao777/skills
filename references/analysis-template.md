# 0. 论文基础信息（MetaData & TL;DR）

- **标题与出处：**[论文名称] | [会议/ArXiv] | [机构/作者背景]
- **一句话电梯演讲：** 用一句话高度概括论文的核心贡献和最终业务/学术价值，要求通俗易懂。例如：“本文提出了一种无需 Critic 模型的 RLHF 变体，通过组相对优势评估来显著降低分布式训练的显存消耗。”

# 1. 动因与问题定义 (Motivation & Problem Statement)

- **现有方法的缺陷 (痛点)：**明确指出当前研究或工业界存在的具体缺陷、未解决的问题或性能瓶颈。例如：目前的基线模型在进行 Agent 长链条推理（Long CoT）时容易产生严重幻觉，或者传统的 PPO 算法在千卡集群上训练时通信开销过大且难以收敛。
- **核心解决思路 (Intuition)：**阐述作者提出解决方案的根本出发点和底层逻辑。例如：“既然单步 Reward 容易被模型通过作弊手段（Hack）骗取高分，作者的直觉是干脆取消单步评估，只在整个 Trajectory 结束后给出一次延迟奖励。”

# 2. 技术与算法设计 (Technical Approach)

- **架构/算法的改动点：** 具体描述本文提出的模型架构或算法流程与经典方法（Baseline）的结构性差异。例如：在标准 Transformer 结构中引入了额外的 Routing 层，或者在策略优化流程中去掉了价值网络（Value Network）。
- **核心数学公式与原理：** 提取最能代表该算法本质的 1-2 个核心数学公式，并用通俗语言解释公式中每个变量的实际物理或几何意义。例如：写下改进后的 GRPO Loss 函数 $J(\theta)$，并说明其中哪一项代表了对输出长度或 KL 散度的惩罚。（如果存在才写，此项不是必需项）
- **数据处理流程：** 说明模型在训练和推理阶段的数据输入格式、处理流程以及输出形式。例如：输入数据必须是按照特定 JSON 格式封装的 Prompt 集合，期望模型输出遵循严格的 Thought-Action-Observation 闭环结构。（如果存在才写，此处不是必须项）

# 3. 工程实现与算力成本 (Engineering & Compute Costs) 

- **训练成本与硬件要求：** 记录复现该算法所需的计算资源消耗规模及硬件配置。例如：使用了 8 节点 64 卡 NPU 集群训练了 72 小时，依赖了特定的序列并行策略
- **代码框架与适配难度：** 评估开源代码生态以及向内部业务框架迁移的难度。例如：官方已开源代码，但若要接入内部的 verl 框架或特定的分布式运行环境，需要重构部分通信逻辑
- **系统开销与推理延迟：** 量化该方法在线上部署时对系统吞吐量和延迟的影响。例如：由于多步采样机制，推理时的首字响应时间（TTFT）增加了约 1.5 秒

（本章不是必须项，如果没有原文没有提及，可以省略）

# 4. 实验设计与核心结论 (Experiments & Conclusions)

- **评测基准与实验设置：** 定义论文使用了哪些数据集、评测指标，以及对比的 Baseline 模型是什么。例如：在 VL-RewardBench 上评估了 Agent 动作执行的成功率，对比了原生的 Qwen3.5-27B 模型。

- **核心实验结论 (Key Conclusions)：** 明确指出实验最终证明了什么事实或推翻了什么假设。例如：实验证明，引入该算法后，模型在长文本处理和多步工具调用上的成功率绝对提升了 15%，且在跨域任务上具备良好的泛化性。

- **关键数据支撑：** 提炼支撑上述结论的最核心对比数据，避免罗列原始表格。例如：“在模型规模同为 30B 时，显存占用比 PPO 降低了 40%，但最终 Reward 得分基本持平。”

# 5. 局限性与跟进建议 (Limitations & Next Steps)

- **方法的局限性与风险 (Limitations)：**结合实际工程经验，指出该方法在落地时可能面临的隐性坑点。例如：该方法极度依赖高质量的人工标注偏好数据，或者在长序列下容易出现奖励过度优化。

- **下一步行动建议 (Action Items)：**为团队或自己提供具有明确导向性的后续跟进策略。例如：① 极具落地价值，建议下周投入资源进行小规模跑通实验；② 整体偏学术，但其 Loss 设计思想可吸收进当前方案；③ 算力要求极高且收益低，建议直接放弃跟进。
